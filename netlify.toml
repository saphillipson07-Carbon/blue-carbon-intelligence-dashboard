[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NPM_FLAGS = "--legacy-peer-deps"

# Vite's app is a single-page app using client-side routing;
# unmatched paths should fall through to index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
